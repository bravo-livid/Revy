--!strict
--!native
--!optimize 2

local Types = require(script.Parent.Types)
type World = Types.World
type EntityBuilder = Types.EntityBuilder

local EntityBuilder = {}

function EntityBuilder.new(world: World): EntityBuilder
   
end

return EntityBuilder